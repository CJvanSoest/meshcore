# mc_common — foundation leaf: constants, view enum, UI-state externs, config
# enums, pax-free emoji table. See docs/wiki/Components.md.
idf_component_register(
    SRCS
        "emoji_table.c"
    INCLUDE_DIRS
        "."
)
