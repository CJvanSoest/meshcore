dependencies:
  idf: ">=5.5.1"
  badgeteam/badge-bsp: "^0.9.9"
  nicolaielectronics/tanmatsu-wifi: "^1.1.2"
  robotman2412/pax-gfx: "^2.1.0"
  nicolaielectronics/tanmatsu-lora:
    git: http://192.168.2.25:3000/CJ/tanmatsu-lora.git
    version: main
    # Gitea fork of upstream HEAD (handle-based API) + local patches:
    #   - per-packet RSSI/SNR stats on PACKET_RX + GET_RSSI_INST (noise floor)
    #   - GET_FW_VERSION query (radio app version string)
    # Pairs with tanmatsu-radio Gitea fork. Upstream PR for RSSI/SNR in
    # review; FW-version is waiting on Renze's system-protocol refactor.
