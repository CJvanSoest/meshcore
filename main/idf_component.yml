dependencies:
  idf: ">=5.5.1"
  badgeteam/badge-bsp: "^0.9.9"
  nicolaielectronics/tanmatsu-wifi: "^1.1.2"
  robotman2412/pax-gfx: "^2.1.0"
  nicolaielectronics/tanmatsu-lora:
    git: http://192.168.2.25:3000/CJ/tanmatsu-lora.git
    version: main
    # Gitea fork of upstream main. RSSI/SNR + GET_RSSI_INST are now MERGED
    # upstream (PR #3). Fork retained only for two not-yet-upstream patches:
    #   - rx_boost field (sensitivity toggle)
    #   - GET_FW_VERSION query (radio app version string)
    # Both wait on Renze's system-protocol/radio-side work; drop this fork
    # and switch to nicolaielectronics/tanmatsu-lora once they land upstream.
    # Pairs with tanmatsu-radio Gitea fork (same two extra commits).
